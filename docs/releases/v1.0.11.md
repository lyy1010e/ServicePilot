# ServicePilot 1.0.11

## 新增

- 新增应用内更新进度卡片，更新下载和安装阶段会显示实时状态，减少更新时的等待感。

## 优化

- 批量启动时，未选中服务的日志会在切换时重新从后端加载，避免启动期间产生的日志被前端空缓存覆盖。
- Spring Boot 服务在检测到本地访问端口或 URL 后也会自动进入运行中，适配启动完成日志不标准或未被捕获的项目。

## 修复

- 修复自定义 `时间|级别|...` 日志格式重复显示时间和级别的问题。
- 修复部分 Spring 服务已经监听端口但界面仍停留在“启动中”的状态识别问题。
