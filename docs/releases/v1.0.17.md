# ServicePilot 1.0.17

## 修复

- 修复“新增服务”选择 Spring Boot 项目目录后无法自动导入的问题。
- 自动识别 Spring Boot 项目时不再错误进入缺少 Main Class 的手动保存流程。
- 自动导入完成后立即刷新服务列表，并显示导入成功提示。

## 验证

- 新增前端回归测试，覆盖选择 Spring Boot 项目后调用自动导入、且不调用普通保存的场景。
