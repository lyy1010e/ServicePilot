# ServicePilot 1.0.14

## 新功能

- 新增“启动时恢复上次退出的服务”设置项，默认关闭。
- 开启后，ServicePilot 会在下次启动时按顺序恢复上次正常退出前仍在运行的本地服务。

## 修复与可靠性

- 恢复清单独立保存并在启动后一次性消费，删除或失效的服务不会阻断其余服务恢复。
- 更新和导入配置只停止服务，不会写入恢复清单，避免更新重启后意外恢复服务。
- 恢复清单采用临时文件写入后替换，降低退出过程中被中断导致清单损坏的风险。

## 验证

- 前端单元测试、Rust 单元测试和 TypeScript 类型检查均已通过。
