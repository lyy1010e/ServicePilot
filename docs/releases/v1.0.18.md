# ServicePilot 1.0.18

## 修复

- 修复“扫描导入”中点击“导入选中服务”后界面持续等待、服务未写入列表的问题。
- 导入 IDEA Spring Boot 项目时只读取一次应用状态设置，避免同一状态锁发生自我等待。
- IDEA 多模块项目仅沿 Maven 声明的模块定位启动类，避免无边界目录扫描造成导入延迟。

## 验证

- TypeScript 类型检查通过。
- 前端单元测试 29 项通过。
- Rust 单元测试 23 项通过，覆盖导入设置快照与 IDEA 多模块定位。
