# Tech Icon Assets

This folder stores local SVG assets for framework and runtime badges shown in service rows.

Keep brand/framework icons as files instead of inline JSX paths. When replacing an icon, prefer official SVGs or trusted open-source sources, optimize the file, and preserve the same kebab-case filename used by the framework mapping in `App.tsx`.
